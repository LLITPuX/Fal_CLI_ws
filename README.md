# Gemini Text Structurer

Модерний full-stack застосунок для перетворення неструктурованого тексту у структурований JSON за допомогою Google Gemini AI.

## Можливості

### Gemini Text Structurer
- **Візуальний конструктор JSON-схем** — створюйте та редагуйте структуру без ризику синтаксичних помилок
- **Атомарне редагування полів** — додавайте/змінюйте/видаляйте окремі поля через зручні форми
- **Збереження шаблонів** — зберігайте улюблені схеми та швидко завантажуйте їх
- **Вибір моделі Gemini** — перемикання між flash (15 RPM) та pro (2 RPM) моделями
- **Автоматичні timestamp** — поля `time`, `timestamp`, `processing_time` заповнюються часом обробки
- **Динамічна валідація** — підтримка користувацьких схем поряд зі стандартною структурою

### FalkorDB Graph Database
- **Система шаблонів вузлів** — створюйте типізовані вузли через готові шаблони
- **8 типів полів** — text, longtext, number, boolean, enum, date, url, email
- **Базові шаблони** — 5 готових шаблонів (Person, Company, Event, Product, Document)
- **Імпорт/експорт шаблонів** — зберігайте та ділітеся шаблонами через JSON файли
- **Міграція вузлів** — автоматичне оновлення існуючих вузлів при зміні шаблону
- **Створення вузлів (Nodes)** — через шаблони з валідацією полів
- **Створення зв'язків (Relationships)** — візуальна форма для графових зв'язків
- **Cypher запити** — повна підтримка Cypher Query Language з прикладами
- **Статистика графа** — real-time метрики (nodes, edges, labels, relationship types)
- **Smart dropdowns** — автоматичне заповнення існуючих labels та relationship types

### Cybersich Chat System (✅ Phase 2 Complete)
- **Мультиагентна архітектура** — побудована на LangGraph
- **Писарь (Clerk Agent)** — записує всі повідомлення в граф `cybersich_chat`
- **Підсвідомість (Subconscious Agent)** — аналізує контекст, знаходить схожі повідомлення, створює embeddings
- **Безкінечна пам'ять** — вся історія чату в FalkorDB, без обмежень контекстного вікна
- **Chat Sessions** — організація розмов за сесіями
- **Message History** — доступ до повної історії з temporal queries
- **Semantic Search** — пошук схожих повідомлень через OpenAI embeddings
- **Entity Extraction** — автоматичне виділення сутностей з повідомлень
- **Готовність до Phase 3** — архітектура для Orchestrator Agent

Детальніше: [CHAT_SYSTEM.md](CHAT_SYSTEM.md)

### Document Archiver (📄 Production Ready)
- **Динамічні схеми** — створюйте власні типи документів з кастомними схемами вузлів
- **Версіонування** — повна історія змін схем та промптів
- **Preview режим** — перегляд графу перед архівуванням
- **Інтеграція з Gemini AI** — автоматична структуризація документів
- **Node Schema Editor** — візуальний редактор схем вузлів
- **Prompt Editor** — управління промптами для різних типів документів
- **Rollback** — повернення до попередніх версій схем та промптів
- **Статистика** — метрики по архівованим документам

Детальніше: [Document Archiver API](http://localhost:3000/api/docs#/archive)

### Технології
- **Асинхронний FastAPI backend** — повністю async, модульна архітектура
- **LangGraph multi-agent system** — оркестрація агентів для чату
- **Контейнеризація Docker Compose** — готове production-рішення з FalkorDB

## Архітектура

```
┌─────────────────┐         ┌──────────────────┐         ┌────────────────┐
│   Frontend      │  HTTP   │     Backend      │  CLI    │  Gemini AI     │
│   (React+TS)    │────────▶│    (FastAPI)     │────────▶│  (Google)      │
│   Port: 3000    │         │    Port: 8000    │         │                │
└─────────────────┘         └────────┬─────────┘         └────────────────┘
        │                            │
        │                            ▼
        │                   ┌─────────────────┐
        │                   │    FalkorDB     │
        │                   │  Graph Database │
        │                   │   Port: 6379    │
        │                   └─────────────────┘
        │
        │    Nginx Reverse Proxy
        └────────────────────────────
```

## Технологічний стек

**Frontend**: React 18, TypeScript (strict mode), Vite, Nginx (production)

**Backend**: FastAPI 0.115+, Python 3.12, Pydantic v2, asyncio

**Інфраструктура**: Docker Compose, Gemini CLI (npm), multi-stage Dockerfile

## Структура проєкту

```
Gemini CLI/
├── backend/
│   ├── app/
│   │   ├── api/              # REST endpoints
│   │   ├── core/             # Config & exceptions
│   │   ├── models/           # Pydantic schemas
│   │   ├── services/         # Business logic
│   │   └── main.py           # FastAPI app
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── services/
│   │   ├── types/
│   │   ├── styles/
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── nginx.conf
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml
├── .env
└── README.md
```

## Швидкий старт

### Передумови
- Docker Desktop (Windows)
- Налаштовані облікові дані Gemini CLI (`gemini configure` у локальній системі)
- Google Cloud Project ID

### Клонування та початкове налаштування

```powershell
git clone <repo-url>
cd "Gemini CLI"

# Скопіювати шаблон конфігурації
Copy-Item env.example .env

# Відкрити для редагування
notepad .env
```

### Конфігурація `.env`

Заповніть ключові змінні середовища:

```ini
HOST_GEMINI_DIR=C:\Users\YourUser\.gemini
GEMINI_CLI=gemini
GEMINI_MODEL=gemini-2.5-flash
GEMINI_MODELS=gemini-2.5-flash,gemini-2.5-pro
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
API_PORT=8000
FRONTEND_PORT=3000
VITE_GEMINI_MODEL=gemini-2.5-flash
```

> **Правила конфігурації моделей**
> - `GEMINI_MODEL` — дефолтне значення для бекенду, якщо запит не передає `model`.
> - `GEMINI_MODELS` — список доступних моделей через кому. Може містити одну або кілька позицій (наприклад, `gemini-2.5-flash,gemini-2.5-pro`).
> - `VITE_GEMINI_MODEL` — модель, попередньо обрана у UI; вона має входити до `GEMINI_MODELS`.
> - Підтримуйте синхронізацію цих трьох змінних при додаванні/видаленні моделей.

### Запуск у Docker

```powershell
# Повна збірка образів
docker compose build --no-cache

# Старт сервісів
docker compose up -d

# Перегляд логів
docker compose logs -f
```

### Доступ до застосунку
- Frontend: http://localhost:3000
- Backend health: http://localhost:3000/api/health
- Swagger UI: http://localhost:3000/api/docs

## Використання

### Document Archiver (Головна сторінка)

1. **Document Archiver** є точкою входу для роботи з документами.
2. Підтримує автоматичне визначення типу документа та відповідного шаблону.
3. **Node Schema Editor** дозволяє візуально редагувати структуру вузла.
4. **Prompt Editor** дозволяє налаштовувати інструкції для AI.
5. **Preview** режим дозволяє переглянути результат перед збереженням.

### Graph Visualization

1. Перейдіть на сторінку `/falkordb` для візуалізації графа.
2. Використовуйте селектор для вибору графа (`gemini_graph`, `cybersich_chat`, `cursor_memory`).
3. Інтерактивний візуалізатор дозволяє досліджувати зв'язки між вузлами.


### FalkorDB: Робота з шаблонами вузлів

**Базові шаблони (автоматично завантажуються при старті):**
- 👤 **Person** — люди та персонажі (name, age, email, biography, type, is_active)
- 🏢 **Company** — організації (name, website, employee_count, founded_date, industry, description)
- 📅 **Event** — події (title, event_date, location, attendees_count, event_type, description, is_online)
- 📦 **Product** — товари (name, price, category, sku, in_stock, quantity, description)
- 📄 **Document** — документи (title, author, created_date, content, tags, document_type, source_url, is_published)

**Створення вузла через шаблон:**
1. Перейдіть на сторінку FalkorDB → таб **"📍 Node"**
2. Оберіть шаблон з випадаючого списку
3. Заповніть поля згідно шаблону (з автоматичною валідацією)
4. Натисніть **"Create Node"**

**Управління шаблонами:**
1. Таб **"📋 Templates"**
2. **Створити**: ➕ Create Template → заповніть Label, Icon, Description, додайте поля
3. **Редагувати**: ✏️ Edit на картці шаблону
4. **Експорт одного**: 📥 Export → завантажує `{label}-template.json`
5. **Експорт всіх**: 📥 Export All → завантажує `node-templates-DATE.json`
6. **Імпорт**: 📤 Import → підтримує обидва формати (один шаблон або масив)
7. **Міграція**: 🔄 Migrate → оновлює існуючі вузли після зміни шаблону
8. **Видалити**: 🗑️ Delete (тільки якщо немає вузлів з цим шаблоном)

**Типи полів шаблонів:**
- **text** — короткий текст (input)
- **longtext** — довгий текст з переносами (textarea)
- **number** — числові значення з валідацією min/max
- **boolean** — так/ні (radio buttons)
- **enum** — випадаюч список з визначеними значеннями
- **date** — вибір дати (date picker)
- **url** — URL адреса з валідацією формату
- **email** — email адреса з валідацією формату

### API

**Health Check**

```bash
curl http://localhost:3000/api/health
```

**Structure Text (базовий запит)**

```powershell
$body = @{
    text = "Неструктурований текст про AI..."
    model = "gemini-2.5-flash"
} | ConvertTo-Json -Compress

Invoke-RestMethod -Uri http://localhost:3000/api/structure `
    -Method Post `
    -ContentType "application/json; charset=utf-8" `
    -Body $body
```

**Structure Text (з користувацькою схемою)**

```powershell
$customSchema = @"
{
  "title": "string - Document title",
  "author": "string - Author name if mentioned",
  "time": "string - Processing timestamp (auto-filled)",
  "summary": "string - Brief summary",
  "tags": ["array of strings - Main keywords"]
}
"@

$body = @{
    text = "Неструктурований текст про AI..."
    model = "gemini-2.5-flash"
    custom_schema = $customSchema
} | ConvertTo-Json -Compress

Invoke-RestMethod -Uri http://localhost:3000/api/structure `
    -Method Post `
    -ContentType "application/json; charset=utf-8" `
    -Body $body
```

> **Примітка:** Поля `time`, `timestamp`, `processing_time` автоматично заповнюються backend часом обробки у форматі `HH:MM:SS`.

## Розробка

### Backend

```powershell
docker compose exec backend bash
docker compose logs -f backend
```

### Frontend

```powershell
cd frontend
npm install
npm run dev
# або всередині контейнера
docker compose exec frontend sh
```

### Перевірка збірки

Перед кожним PR/комітом фронтенду виконуйте продакшн-збірку:

```powershell
npm run build
```

Цей самий крок рекомендується додати в CI, щоб виявляти `tsc`/Vite-помилки та невикористані символи.

## Усунення несправностей

### Типові помилки

**Model '...' is not allowed**
- Перевірте, що вибрана модель додана до `GEMINI_MODELS` у `.env`
- Перезберіть контейнери: `docker compose up -d --build backend`

**CLI timeout (5 хвилин)**
- Збільшіть `gemini_timeout` у `backend/app/core/config.py`
- Рестартуйте бекенд: `docker compose restart backend`

**Поле `time` повертає `null`**
- Це очікувана поведінка: поле автоматично заповнюється backend часом обробки
- Якщо все одно `null` — перевірте, що контейнер перезібраний після останніх змін

**Немає доступу до Gemini API**
- Переконайтеся, що облікові дані змонтовані у `HOST_GEMINI_DIR`
- Запустіть `gemini configure` локально для налаштування
- Перевірте `GOOGLE_CLOUD_PROJECT` у `.env`

**Візуальний редактор не зберігає поля**
- Перевірте консоль браузера (F12) на JavaScript-помилки
- Очистіть localStorage: `localStorage.clear()` у консолі браузера
- Перезавантажте сторінку

## Моніторинг та логи

```powershell
# Статус контейнерів
docker compose ps

# Health-перевірки всередині контейнерів
docker compose exec backend curl http://localhost:8000/health
docker compose exec frontend wget -q -O- http://localhost:80

# Поточні логи
docker compose logs -f
docker compose logs -f backend
docker compose logs -f frontend
```

## Збереження даних
Результати зберігаються у volume `backend-data`.

```powershell
docker volume ls
docker volume inspect gemini-cli_backend-data
docker run --rm -v gemini-cli_backend-data:/data -v ${PWD}:/backup alpine `
  tar czf /backup/data-backup.tar.gz /data
```

## Безпека
- запуск сервісів під non-root користувачем у контейнерах
- монтування credential-директорій лише в режимі read-only
- CORS налаштований під фронтенд
- винятки та логування без чутливих даних
- Pydantic гарантує валідацію введення

## Внесок
1. Дотримуйтеся SOLID та асинхронних практик.
2. Пишіть типи (Python typing / TypeScript strict).
3. Додавайте тести для нової логіки.
4. Оновлюйте документацію та приклади.
5. Перевіряйте `npm run build` перед створенням PR.

## Ліцензія
MIT License

---

**Версія:** 2.3.0  
**Останнє оновлення:** 25 листопада 2025 року
