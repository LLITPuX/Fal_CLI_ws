# 🚀 FalkorDB Quick Start

## Швидкий запуск за 3 кроки

### 1️⃣ Переконайтеся, що у вас є `.env` файл

```bash
# Якщо немає - створіть з env.example
copy env.example .env
```

Перевірте, що є FalkorDB налаштування:
```env
FALKORDB_HOST=falkordb
FALKORDB_PORT=6379
FALKORDB_GRAPH_NAME=gemini_graph
FALKORDB_MAX_QUERY_TIME=30
```

### 2️⃣ Запустіть Docker Compose

```powershell
# Зупинити попередні контейнери (якщо потрібно)
docker compose down

# Збілдити та запустити всі сервіси
docker compose up --build
```

Зачекайте поки всі сервіси стануть healthy (~30-60 секунд).

### 3️⃣ Відкрийте браузер

```
http://localhost:3000
```

Натисніть на **"🔗 FalkorDB"** в навігації зверху.

---

## 📝 Швидкий тест

### Крок 0: Перевірте шаблони

1. На сторінці FalkorDB виберіть таб **"📋 Templates"**
2. Побачите 5 базових шаблонів:
   - 👤 **Person** - люди та персонажі
   - 🏢 **Company** - організації
   - 📅 **Event** - події
   - 📦 **Product** - товари
   - 📄 **Document** - документи

💡 Якщо шаблонів немає - вони автоматично завантажаться при першому запуску backend.

### Створіть ваш перший вузол

1. Виберіть таб **"📍 Node"**
2. Оберіть шаблон **"👤 Person"** з випадаючого списку
3. Заповніть поля:
   - **Ім'я**: `Alice` (обов'язково)
   - **Вік**: `25` (опціонально)
   - **Email**: `alice@example.com` (опціонально)
   - **Тип персони**: виберіть `individual` зі списку
   - **Активний**: Yes
4. Натисніть **"Create Node"**

✅ Праворуч у статистиці побачите: **1 Node**, **1 Label** (Person)

### Створіть другий вузол

1. Оберіть шаблон **"👤 Person"** (той самий)
2. Заповніть поля:
   - **Ім'я**: `Bob`
   - **Вік**: `30`
   - **Email**: `bob@example.com`
   - **Тип персони**: `employee`
3. Натисніть **"Create Node"**

✅ Статистика: **2 Nodes**, **1 Label**

### Створіть зв'язок

1. Перейдіть на таб **"🔗 Relationship"**
2. Заповніть **📍 From Node**:
   - Label: `Person` (виберіть з dropdown)
   - Properties: Key: `name`, Type: **Text**, Value: `Alice`
3. Заповніть **🔗 Relationship**:
   - Type: `KNOWS` (введіть custom або виберіть existing якщо є)
   - Properties (опціонально): Key: `since`, Type: **Number**, Value: `2020`
4. Заповніть **📍 To Node**:
   - Label: `Person` (виберіть з dropdown)
   - Properties: Key: `name`, Type: **Text**, Value: `Bob`
5. Натисніть **"Create Relationship"**

✅ Статистика: **1 Relationship**, **1 Relation Type** (KNOWS)

### Виконайте запит

1. Перейдіть на таб **"🔍 Query"**
2. Введіть Cypher запит:
   ```cypher
   MATCH (p:Person) RETURN p
   ```
3. Або виберіть один з прикладів нижче
4. Натисніть **"Execute Query"**
5. Перегляньте результати!

---

## 🎯 Приклади Cypher запитів

### Базові

```cypher
-- Всі вузли
MATCH (n) RETURN n LIMIT 10

-- Всі люди
MATCH (p:Person) RETURN p

-- Знайти конкретну людину
MATCH (p:Person {name: "Alice"}) RETURN p

-- Всі зв'язки
MATCH (a)-[r]->(b) RETURN a, r, b LIMIT 10
```

### Складніші

```cypher
-- Знайти друзів Alice
MATCH (alice:Person {name: "Alice"})-[:KNOWS]->(friend)
RETURN friend.name, friend.city

-- Люди з Києва
MATCH (p:Person {city: "Kyiv"})
RETURN p.name, p.age

-- Кількість друзів кожної людини
MATCH (p:Person)-[:KNOWS]->(friend)
RETURN p.name, count(friend) as friends_count
ORDER BY friends_count DESC
```

---

## 📊 Статистика

Права панель показує:
- 📈 Кількість вузлів (Nodes)
- 🔗 Кількість зв'язків (Relationships)
- 🏷️ Всі Labels
- 🔀 Всі типи зв'язків

Натисніть 🔄 для оновлення.

---

## 🔧 Troubleshooting

### ❌ "Failed to connect to FalkorDB"

```powershell
# Перевірити статус
docker compose ps

# Перезапустити FalkorDB
docker compose restart falkordb

# Перевірити логи
docker compose logs falkordb
```

### ❌ Frontend не відкривається

```powershell
# Перевірити порт
docker compose ps frontend

# Перевірити логи
docker compose logs frontend
```

### ❌ Backend помилки

```powershell
# Перевірити логи backend
docker compose logs backend -f

# Перезапустити backend
docker compose restart backend
```

---

## 📋 Управління шаблонами

### Створення власного шаблону

1. Перейдіть на таб **"📋 Templates"**
2. Натисніть **"➕ Create Template"**
3. Заповніть інформацію:
   - **Label**: `Product`
   - **Icon**: `📦`
   - **Description**: "Товар або послуга для продажу"
4. Додайте поля (**➕ Add Field**):
   - `name` (Text, обов'язкове)
   - `price` (Number, опціонально)
   - `category` (Enum: electronics, clothing, food)
5. Натисніть **"Create Template"**

### Імпорт/Експорт

- **📥 Export** (на картці) → `{label}-template.json`
- **📥 Export All** → всі шаблони в один файл
- **📤 Import** → підтримує обидва формати

## 📚 Наступні кроки

- 📖 Детальна документація: [FALKORDB_GUIDE.md](./FALKORDB_GUIDE.md)
- 📋 Система шаблонів: [TEMPLATE_SYSTEM_IMPLEMENTATION.md](./TEMPLATE_SYSTEM_IMPLEMENTATION.md)
- 🌐 API Docs: http://localhost:8000/docs
- 📊 FalkorDB Docs: https://docs.falkordb.com/

---

**Готово! Користуйтеся FalkorDB графовою базою даних! 🎉**

